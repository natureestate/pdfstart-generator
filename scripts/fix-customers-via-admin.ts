/**
 * Script สำหรับอัปเดต lastUsedAt ให้กับลูกค้าที่มีอยู่แล้ว
 * Run with: npx tsx scripts/fix-customers-via-admin.ts
 */

import { initializeApp, cert } from 'firebase-admin/app';
import { getFirestore } from 'firebase-admin/firestore';

// ต้องมี service account key ถึงจะใช้งานได้
// แต่เนื่องจากเราไม่มี ให้ใช้วิธีอื่นแทน

console.log('❌ Script นี้ต้องการ Firebase Admin SDK Service Account Key');
console.log('📝 แนะนำให้ใช้วิธีอื่นแทน:');
console.log('   1. บันทึกลูกค้าใหม่อีกครั้ง (code มี lastUsedAt แล้ว)');
console.log('   2. หรือลบลูกค้าเก่าแล้วสร้างใหม่');

