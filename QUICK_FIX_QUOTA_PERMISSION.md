# ⚡ แก้ไขด่วน: Missing or insufficient permissions (Quota)

## 🚨 ปัญหา
```
❌ ดึงข้อมูล quota ล้มเหลว: FirebaseError: Missing or insufficient permissions.
```

## ✅ วิธีแก้ไข (3 ขั้นตอน)

### ขั้นตอนที่ 1: Deploy Firestore Rules ใหม่ ✅

```bash
firebase deploy --only firestore:rules
```

> **หมายเหตุ:** Rules ถูกแก้ไขแล้วให้อนุญาตเจ้าของบริษัทอ่าน quota ได้

### ขั้นตอนที่ 2: สร้าง Quota สำหรับบริษัท

เปิดไฟล์นี้ใน browser:

```
scripts/auto-create-quota.html
```

**วิธีใช้:**
1. เปิดไฟล์ใน browser (ต้อง login ก่อน)
2. คลิก **"ตรวจสอบสถานะ"** เพื่อดูว่าบริษัทไหนยังไม่มี quota
3. คลิก **"สร้าง Quota ทั้งหมด"** เพื่อสร้าง quota อัตโนมัติ

### ขั้นตอนที่ 3: รีเฟรชหน้าเว็บ

กด `Ctrl+Shift+R` (Windows) หรือ `Cmd+Shift+R` (Mac) เพื่อรีเฟรชหน้าเว็บ

---

## 🎯 ผลลัพธ์ที่คาดหวัง

✅ ไม่มี error `Missing or insufficient permissions` อีกต่อไป  
✅ ข้อมูล quota แสดงผลได้ปกติ  
✅ ระบบทำงานได้เต็มรูปแบบ

---

## 📚 เอกสารเพิ่มเติม

- [QUOTA_PERMISSION_FIX.md](./QUOTA_PERMISSION_FIX.md) - คู่มือแก้ไขแบบละเอียด
- [DYNAMIC_PLAN_GUIDE.md](./DYNAMIC_PLAN_GUIDE.md) - คู่มือจัดการแผนการใช้งาน

---

## 🆘 ถ้ายังมีปัญหา

1. ตรวจสอบว่า deploy rules สำเร็จหรือไม่
2. ตรวจสอบว่าสร้าง quota สำเร็จหรือไม่ (เปิด Firebase Console > Firestore > companyQuotas)
3. ตรวจสอบ Console ใน browser (F12) เพื่อดู error message

